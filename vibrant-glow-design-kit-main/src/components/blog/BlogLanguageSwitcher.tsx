
import React from 'react';

// Language switching is now handled in the main header navigation
// This component is kept for backwards compatibility but doesn't render anything
const BlogLanguageSwitcher = () => {
  return null;
};

export default BlogLanguageSwitcher;
